@{
    ViewBag["List"] = "MaListe";
}

<div class="container p-3">
    <div class="row">
        @foreach (var item in this.ViewBag.MaListe)
        {
            <div>
                <h3> @item.Name</h3>
                <h5> @item.Point</h5>
            </div>
        }
    </div>
</div>

